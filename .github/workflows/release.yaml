name: Release

on:
  push:
    tags:
        - 'v*'


permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact_name: peek_tree-linux
          - os: macos-latest
            artifact_name: peek_tree-macos

    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: make -C peek_tree/src

      - name: Create Release Asset
        run: |
          tar czf ${{ matrix.artifact_name }}.tar.gz -C peek_tree/src peek_tree

      - name: Upload Release Asset
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}.tar.gz

      - name: Calculate SHA256 Checksum
        if: matrix.os == 'macos-latest'
        id: checksum
        run: echo "sha256=$(shasum -a 256 ${{ matrix.artifact_name }}.tar.gz | awk '{ print $1 }')" >> $GITHUB_OUTPUT

    outputs:
      sha256: ${{ steps.checksum.outputs.sha256 }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v2

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ github.run_number }}
          release_name: Release v${{ github.run_number }}
          draft: false
          prerelease: false

      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./peek_tree-linux/peek_tree-linux.tar.gz
          asset_name: peek_tree-linux.tar.gz
          asset_content_type: application/gzip

      - name: Update Homebrew Formula
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # or use PAT: ${{ secrets.PAT_TOKEN }}
        run: |
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          # Need to re-checkout with token
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git
          git checkout main
          sed -i.bak "s|url.*|url \"https://github.com/aymanhki/peek_tree/releases/download/v${{ github.run_number }}/peek_tree-macos.tar.gz\"|" ./dist/Homebrew/peek_tree.rb
          sed -i.bak "s|sha256.*|sha256 \"${{ needs.build.outputs.sha256 }}\"|" ./dist/Homebrew/peek_tree.rb
          git add ./dist/Homebrew/peek_tree.rb
          git commit -m "Update Homebrew formula for v${{ github.run_number }}"
          git push






